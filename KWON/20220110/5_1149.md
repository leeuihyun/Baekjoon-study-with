#문제 주소
https://www.acmicpc.net/problem/1149

#문제 풀이 방법
1. 처음엔 전부 입력을 받고, 필요 RGB를 최솟값으로 정렬한 후 도출했지만 다른 경우의 수가 있어 실패했다.
2. 다음엔 구하고자 하는 index의 다음 index와 더한 값들 중 최솟값을 찾아 도출했지만 다른 경우의 수가 있어 실패했다.
3. 입력을 받으며 그 전값들과 비교해 최솟값을 도출해 출력했다.

#시간복잡도
O(N)